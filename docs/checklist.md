# Release Checklist

- Run `npm run lint`.
- Run `npm run build`.
- Confirm page source contains `base:app_id`.
- Confirm only three wallet buttons render.
- Confirm the invite URL is copied but not displayed.
- Confirm `sendKarma` opens a transaction on Base.
- Confirm Basescan input data ends with the configured data suffix.
